/** Base JAX-RS (inclut le préfixe `/api` côté Quarkus). Convention des chemins : `src/app/core/api-conventions.ts`. */
export const environment = {
  production: false,
  apiUrl: 'http://127.0.0.1:8080/api'
};
